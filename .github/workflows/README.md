# GitHub Actions cost guard

このディレクトリには、意図的に実行可能な `.yml` / `.yaml` workflowを置きません。

このrepositoryではGitHub Actionsの従量課金回避を最優先とし、検証は原則としてローカルPC / Remote Desktop Commanderで実行します。

過去のworkflow定義は `.github/workflows-disabled/` に参照用として退避しています。

Actionsを再導入する場合は、先に運用方針を見直し、明示承認を得てください。
